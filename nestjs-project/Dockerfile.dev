FROM node:25.6.0-slim

# ffmpeg/ffprobe are needed so the video processor integration test can run the
# real toolchain inside the API container test suite.
RUN apt update && apt install -y procps curl ffmpeg && rm -rf /var/lib/apt/lists/*

WORKDIR /home/node/app

USER node

CMD [ "tail", "-f", "/dev/null" ] 